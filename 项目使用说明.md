# SteamPY 游戏交易平台 - 项目使用说明

## 📁 项目位置

```
e:\互联网应用开发技术\sitel\计算机23007-23001010719-陈俊旭\steampy-vue
```

---

## 🚀 快速启动（3步）

### 第1步：打开终端，进入项目目录

```powershell
cd "e:\互联网应用开发技术\sitel\计算机23007-23001010719-陈俊旭\steampy-vue"
```

### 第2步：安装依赖（只需运行一次）

```powershell
npm install
```

> ⚠️ 如果之前已经运行过 `npm install`，这步可以跳过。
> 如果依赖包丢失（node_modules文件夹没了），必须重新运行。

### 第3步：启动开发服务器

```powershell
npm run dev
```

启动成功后会显示类似：

```
  VITE v5.4.21  ready in 550 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

然后在浏览器打开 **http://localhost:5173/** 即可访问。

---

## 📦 其他命令

| 命令 | 作用 | 何时使用 |
|------|------|----------|
| `npm run dev` | 启动开发服务器（热更新） | **日常开发/调试** |
| `npm run build` | 构建生产版本 | 答辩前打包、部署 |
| `npm run preview` | 预览构建后的生产版本 | 检查build产物是否正常 |

---

## 🏗️ 项目结构

```
steampy-vue/
├── index.html              # 入口HTML（不要删！）
├── package.json            # 依赖和脚本配置
├── vite.config.ts          # Vite构建配置
├── tsconfig.json           # TypeScript配置
├── env.d.ts                # 全局类型声明
│
├── public/                 # 静态资源（不经过Vite处理）
│   ├── cdk_games.json      # ⭐ 本地游戏数据（备用）
│   ├── slider-data.json    # ⭐ 轮播图数据
│   ├── user-settings.json  # ⭐ 用户配置数据
│   └── picture/            # 游戏图片
│
└── src/                    # 源代码
    ├── main.ts             # 应用入口
    ├── App.vue             # 根组件
    ├── style.css           # 全局样式
    │
    ├── config/
    │   └── supabase-local.ts  # ⭐ 数据库连接（实际生效）
    │
    ├── router/
    │   └── index.ts        # ⭐ 路由配置（12个路径）
    │
    ├── components/         # 12个自定义组件
    │   ├── Layout.vue          # 主布局（顶部导航+底部栏）
    │   ├── GameCard.vue        # 游戏卡片（props+emit+slot）
    │   ├── SearchBar.vue       # 搜索框
    │   ├── Pagination.vue      # 分页器
    │   ├── EmptyState.vue      # 空状态提示
    │   ├── LoadingSpinner.vue  # 加载动画
    │   ├── PriceTag.vue        # 价格标签
    │   ├── TabNav.vue          # 标签导航
    │   ├── Toast.vue           # 消息提示
    │   └── ...
    │
    └── views/              # 15个页面
        ├── Home.vue                # ⭐ 首页（轮播+公告+推荐）
        ├── Login.vue               # ⭐ 登录/注册
        ├── CDKeyMarket.vue         # CDKey国区市场
        ├── BalancePurchase.vue     # 余额购商城
        ├── BalancePurchaseDetail.vue # 余额购详情页
        ├── GiftPurchase.vue        # 礼物代购
        ├── GameDetail.vue          # ⭐ 游戏详情页（动态路由）
        ├── BuyerCenter.vue         # 买家中心（订单）
        ├── SellerCenter.vue        # 卖家中心
        ├── MyGames.vue             # 我的游戏库
        ├── TransactionHistory.vue  # 交易记录
        ├── Recharge.vue            # 充值页面
        ├── AccountSettings.vue     # 账号设置
        └── TestCORS.vue            # CORS测试
```

---

## 🗄️ 数据库信息

### 连接配置

项目使用 **Supabase** 云数据库，通过 Axios 调用 RESTful API。

| 项目 | 值 |
|------|-----|
| 数据库地址 | `https://jffospwfgajablevtnqu.supabase.co` |
| API Key | `sb_publishable_pDsZKZSNQSlKbDYRsz2t_Q_FVznIhu0` |
| 连接方式 | Axios + RESTful API |
| 配置文件 | `src/config/supabase-local.ts` |

### 数据库表

| 表名 | 用途 |
|------|------|
| users | 用户账号 |
| games | 游戏数据 |
| orders | 订单记录 |
| user_wallets | 用户钱包余额 |
| transactions | 交易流水 |
| announcements | 平台公告 |
| user_games | 已购游戏库 |
| seller_quota | 卖家额度 |

### 数据库降级策略

> 当 Supabase 数据库无法连接时，系统会**自动降级**，从 `public/` 目录下的 JSON 文件读取数据，保证页面正常显示。

---

## 🔑 测试账号

| 用户名 | 密码 | 说明 |
|--------|------|------|
| （需自己注册） | - | 登录页支持注册新账号 |

> 💡 打开登录页 http://localhost:5173/login ，点击"注册"创建新账号。

---

## 🛣️ 页面路由一览

| 地址 | 页面 | 类型 |
|------|------|------|
| `/` | 首页 | 一级页面 |
| `/login` | 登录/注册 | 功能页 |
| `/cdkey` | CDKey国区市场 | 二级页面 |
| `/balance` | 余额购商城 | 二级页面 |
| `/balance/detail/:game` | 余额购详情 | 三级页面（动态参数） |
| `/gift` | 礼物代购 | 二级页面 |
| `/game/:id` | 游戏详情页 | 三级页面（动态参数） |
| `/buyer` | 买家中心 | 二级页面 |
| `/seller` | 卖家中心 | 二级页面 |
| `/mygames` | 我的游戏库 | 二级页面 |
| `/transactions` | 交易记录 | 二级页面 |
| `/recharge` | 充值页面 | 功能页 |
| `/settings` | 账号设置 | 功能页 |

---

## ⚠️ 常见问题

### Q1: npm install 报错？
```powershell
# 尝试清除缓存后重装
npm cache clean --force
rm -r node_modules
rm package-lock.json
npm install
```

### Q2: npm run dev 后白屏？
- 打开浏览器控制台（F12）看是否有报错
- 检查 node_modules 是否完整
- 尝试删除 node_modules 重新 install

### Q3: 数据库连接失败？
- 检查网络是否正常
- 确认 Supabase 地址是否可访问
- 系统会自动降级到本地 JSON 数据，不影响浏览

### Q4: 端口被占用？
```powershell
# Windows：查找占用端口的进程
netstat -ano | findstr :5173
# 然后结束该进程
taskkill /PID 进程号 /F
```

### Q5: 修改代码后页面没更新？
- 保存文件（Ctrl+S）
- Vite 支持热更新，保存后会自动刷新
- 如果没反应，手动刷新浏览器（Ctrl+F5）

---

## 📊 项目技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4 | 前端框架 |
| TypeScript | 6.0 | 类型安全 |
| Vite | 5.1 | 构建工具 |
| Vue Router | 4.3 | 路由管理 |
| Axios | 1.17 | HTTP请求 |
| Vant UI | 3.6 | UI组件库（25种组件） |
| Supabase | - | 云数据库 |

---

## 🧹 废弃文件（可以忽略，不影响运行）

以下文件是之前开发过程中产生的旧版本，**已不再被引用**：

```
src/config/supabase.js          # 旧 Supabase SDK 版本
src/config/supabase-fetch.js    # 旧 fetch 版本
src/config/supabase-proxy.js    # 旧代理版本
src/utils/supabase.js           # 旧工具函数
src/utils/supabase-proxy.js     # 旧代理工具函数
src/counter.ts                  # 测试文件
```

如果想清理，可以直接删除这些文件。

---

## ✅ 构建验证

```powershell
npm run build
```

构建成功会显示：
```
✓ 373 modules transformed.
✓ built in 14.18s
```

---

**祝使用顺利！** 🎮
