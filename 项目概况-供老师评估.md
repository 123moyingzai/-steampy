# SteamPY 游戏交易平台 - 项目概况

> 本文档用于向指导老师汇报项目情况，评估是否适合作为实训学期小组项目继续开发。

---

## 一、项目基本信息

| 项目 | 内容 |
|------|------|
| **项目名称** | SteamPY 游戏交易平台 |
| **项目类型** | 电商类 Web 前端应用 |
| **当前状态** | 可运行、可访问、可演示（MVP 已完成） |
| **代码量** | 前端 25 个 Vue 组件 + 4 个 TS 配置文件 + 3 个 JSON 数据文件 |
| **数据库** | Supabase 云数据库（8 张业务表） |
| **构建工具** | Vite |
| **开发模式** | 单人开发，已预留多人协作空间 |

---

## 二、使用的技术栈

### 前端框架与语言

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue 3** | 3.4.21 | 核心前端框架，使用 Composition API + `<script setup>` 语法糖 |
| **TypeScript** | 6.0.3 | 全项目类型安全，接口定义、函数参数/返回值类型约束 |
| **Vite** | 5.1.6 | 下一代前端构建工具，开发热更新 + 生产打包 |

### 路由与状态

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue Router** | 4.3.0 | 前端路由管理，12 个路径，含 2 个动态参数路由 |
| **sessionStorage** | - | 用户登录状态持久化（已登录则保持，关闭浏览器清除） |

### HTTP 请求

| 技术 | 版本 | 用途 |
|------|------|------|
| **Axios** | 1.17.0 | 统一封装 HTTP 请求，连接 Supabase RESTful API，统一错误处理 |

### UI 组件库

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vant UI** | 3.6.16 | 移动端优先的 UI 组件库，已引入 25 种组件 |

### 后端/数据库

| 技术 | 用途 |
|------|------|
| **Supabase** | 云数据库服务（PostgreSQL），通过 RESTful API 进行 CRUD 操作 |
| **Supabase Storage** | 对象存储（预留，可用于图片上传） |

### 其他工具

| 技术 | 用途 |
|------|------|
| **npm** | 包管理 |
| **ES Modules** | 模块化方案（`import/export`） |

---

## 三、项目特点

### 1. 三级页面结构（符合大作业要求）

| 层级 | 页面 | 说明 |
|------|------|------|
| **一级页面（首页）** | Home.vue | 平台入口，轮播图 + 公告 + 游戏推荐 |
| **二级页面（栏目页）** | CDKeyMarket / BalancePurchase / GiftPurchase / BuyerCenter / SellerCenter | 各业务模块入口 |
| **三级页面（内容页）** | GameDetail / BalancePurchaseDetail / AccountSettings | 具体详情/功能操作页 |

### 2. 完整的电商业务流程

```
用户注册 → 登录 → 浏览游戏 → 查看详情 → 购买 → 生成订单 → 查看交易记录 → 管理游戏库
                                                                    ↘ 账号设置
```

### 3. 双数据源降级策略

```
优先：Supabase 云数据库（在线持久化）
  ↓ 失败降级
备用：本地 JSON 文件（public/*.json）
```

保证数据库不可用时页面仍可正常浏览，系统健壮性更强。

### 4. TypeScript 全栈类型安全

- 所有 `.js` 已迁移为 `.ts`
- 所有 `.vue` 已添加 `lang="ts"`
- 定义了 User、Game、Order、Transaction、Wallet 等全局接口
- API 函数有完整的参数和返回值类型约束

### 5. 模块化 API 设计

统一封装在 `supabase-local.ts`，按业务模块划分为：

| API 模块 | 功能 |
|---------|------|
| authAPI | 注册、登录、登出、更新用户信息 |
| gameAPI | 获取游戏列表、按ID查游戏 |
| orderAPI | 创建订单、查询用户订单 |
| walletAPI | 查询/更新钱包余额 |
| transactionAPI | 创建交易记录、查询流水 |
| announcementAPI | 获取公告列表 |
| userGameAPI | 查询用户游戏库、添加游戏、激活游戏 |
| sellerAPI | 查询卖家额度 |

### 6. 自定义组件体系（12 个）

| 组件 | 使用的特性 | 复用场景 |
|------|-----------|----------|
| Layout.vue | props + slot | 全局布局容器 |
| GameCard.vue | props + emit + slot | 所有游戏列表页 |
| SearchBar.vue | props + emit | 市场类页面搜索 |
| Pagination.vue | props + emit | 列表分页 |
| EmptyState.vue | props + slot | 空数据提示 |
| LoadingSpinner.vue | props | 加载状态 |
| PriceTag.vue | props | 价格展示 |
| TabNav.vue | props + emit | 分类导航 |
| Toast.vue | props + emit | 消息提示 |

### 7. Vant UI 组件库应用（25 种）

Button、Field、CellGroup、Tabbar、TabbarItem、NavBar、Icon、Loading、Empty、Tag、Badge、Divider、Toast、Dialog、ActionSheet、Popup、SwipeCell、Checkbox、CheckboxGroup、Radio、RadioGroup、Switch、Slider、Stepper、Progress、Circle、NoticeBar、Image

### 8. 响应式设计

- 使用 rem/vw 单位
- Flexbox 自适应布局
- 支持移动端和 PC 端访问

---

## 四、当前功能完成度

| 模块 | 功能 | 状态 | 说明 |
|------|------|------|------|
| **用户系统** | 注册 | ✅ 已完成 | 用户名+密码注册 |
| | 登录 | ✅ 已完成 | sessionStorage 持久化 |
| | 退出 | ✅ 已完成 | 清除会话 |
| | 账号设置 | ✅ 已完成 | 修改昵称、密码等 |
| **商品展示** | 首页推荐 | ✅ 已完成 | 轮播图 + 游戏列表 |
| | CDKey 市场 | ✅ 已完成 | 分类 + 搜索 + 分页 |
| | 余额购 | ✅ 已完成 | 商品展示 + 详情页 |
| | 礼物代购 | ✅ 已完成 | 商品列表 |
| | 游戏详情 | ✅ 已完成 | 动态路由 /game/:id |
| **交易系统** | 购买下单 | ✅ 已完成 | 订单写入数据库 |
| | 买家中心 | ✅ 已完成 | 订单列表（CDKey/余额购分类） |
| | 订单状态 | ✅ 已完成 | 待支付/已完成等 |
| | 交易记录 | ✅ 已完成 | 资金流水明细 |
| | 充值 | ✅ 已完成 | 钱包余额充值 |
| | 我的游戏库 | ✅ 已完成 | 已购游戏列表 |
| **后台/卖家** | 卖家中心 | ⚠️ 基础完成 | 框架已搭，功能待扩展 |
| **数据库** | Supabase 连接 | ✅ 已完成 | Axios RESTful API |
| | 本地降级 | ✅ 已完成 | JSON 文件备用 |

---

## 五、数据库表结构（8 张表）

| 表名 | 字段 | 说明 |
|------|------|------|
| **users** | id, username, password_hash, nickname, phone, user_type, created_at | 用户账号 |
| **games** | id, name, price, original_price, discount, image, image_url, description, stock, is_presale | 游戏商品 |
| **orders** | id, order_no, buyer_id, game_id, game_name, price, quantity, total_price, status, order_type, cdkey, created_at | 订单 |
| **user_wallets** | id, user_id, balance, frozen_balance, created_at | 钱包 |
| **transactions** | id, transaction_no, user_id, type, title, amount, balance_before, balance_after, status, created_at | 交易流水 |
| **announcements** | id, title, content, publish_date, is_top, is_active | 公告 |
| **user_games** | id, user_id, order_id, game_id, game_name, cdkey, version, status, purchase_date | 游戏库 |
| **seller_quota** | seller_id, quota, used | 卖家额度 |

---

## 六、JSON 数据文件（3 处）

| 文件 | 内容 | 用途 |
|------|------|------|
| `public/cdk_games.json` | 12 款游戏信息 | 游戏数据备用源 |
| `public/slider-data.json` | 轮播图配置 | 首页轮播图数据 |
| `public/user-settings.json` | 用户类型/支付方式/订单状态 | 系统配置数据 |

---

## 七、可扩展方向（小组项目可继续开发）

| 方向 | 难度 | 说明 |
|------|------|------|
| **购物车功能** | ⭐⭐ | 前端状态管理 + 新增 cart 表 |
| **商品搜索筛选** | ⭐⭐ | 按价格/分类/折扣筛选 |
| **用户评价系统** | ⭐⭐⭐ | 新增 reviews 表 + 评价组件 |
| **真实支付接入** | ⭐⭐⭐⭐ | 支付宝/微信支付沙箱对接 |
| **消息通知** | ⭐⭐ | 站内信 + WebSocket 实时推送 |
| **商品管理后台** | ⭐⭐⭐ | 管理员增删改查游戏 |
| **Supabase Auth** | ⭐⭐ | 替换当前自建登录，用官方认证 |
| **Supabase Storage** | ⭐⭐ | 游戏封面/用户头像上传 |
| **单元测试** | ⭐⭐ | Vitest + Vue Test Utils |
| **E2E 测试** | ⭐⭐⭐ | Playwright / Cypress |
| **部署上线** | ⭐⭐ | Vercel / Netlify 免费托管 |

---

## 八、项目优势

1. **技术栈主流** - Vue 3 + TypeScript + Vite + Axios 是当前企业级前端开发的主流组合
2. **代码可运行** - `npm run build` 构建成功，无报错
3. **数据库连接** - 真实云数据库，不是 mock 数据
4. **业务完整** - 覆盖用户认证、商品展示、下单购买、订单管理、资金流水
5. **扩展性强** - 架构清晰，可多人分工继续开发
6. **文档齐全** - 已有使用说明 + 答辩指导

---

## 九、项目目录结构

```
steampy-vue/
├── index.html                    # 入口 HTML
├── package.json                  # 依赖配置
├── vite.config.ts                 # Vite 配置
├── tsconfig.json                  # TypeScript 配置
├── env.d.ts                       # 类型声明
│
├── public/                        # 静态资源
│   ├── cdk_games.json            # 游戏数据（备用）
│   ├── slider-data.json          # 轮播图数据
│   ├── user-settings.json       # 用户配置数据
│   └── picture/                  # 游戏图片
│
└── src/
    ├── main.ts                    # 应用入口
    ├── App.vue                    # 根组件
    ├── style.css                  # 全局样式
    ├── config/
    │   └── supabase-local.ts     # ⭐ 数据库连接 + API 封装
    ├── router/
    │   └── index.ts              # 路由配置（12 个路径）
    ├── components/                # 12 个自定义组件
    │   ├── Layout.vue
    │   ├── GameCard.vue
    │   ├── SearchBar.vue
    │   └── ...
    └── views/                     # 15 个页面
        ├── Home.vue
        ├── Login.vue
        ├── GameDetail.vue
        ├── BuyerCenter.vue
        └── ...
```

---

## 十、如何运行项目

```powershell
# 进入项目目录
cd "e:\互联网应用开发技术\sitel\计算机23007-23001010719-陈俊旭\steampy-vue"

# 安装依赖（首次）
npm install

# 启动开发服务器
npm run dev

# 浏览器访问
# http://localhost:5173/
```

---

**以上是项目的完整概况，恳请老师评估是否适合作为实训学期小组项目继续开发。**
